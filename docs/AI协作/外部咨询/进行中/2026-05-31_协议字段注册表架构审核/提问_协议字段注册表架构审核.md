# 提问：协议字段注册表架构审核

时间：2026-05-31

仓库：`zhanglongxiao111/html-indesign`

分支：`main`

提交：`033da5c`

目标文档：

- `docs/superpowers/specs/2026-05-31-protocol-field-registry-architecture-design.md`
- 关联入口：`AGENTS.md`
- 关联长期规范：`docs/规范/HTML_INDESIGN_LIBRARY_SPEC.md`、`docs/规范/SEMANTIC_PROTOCOL.md`、`docs/规范/LABEL_PROTOCOL.md`、`docs/规范/REVERSE_EXPORT.md`

实际发送给 GPT Pro 的提示词：

```text
请使用已连接的 GitHub 连接器审查 `zhanglongxiao111/html-indesign` 仓库，分支 `main`，提交 `033da5c`。

首要目标：审核 `docs/superpowers/specs/2026-05-31-protocol-field-registry-architecture-design.md` 这份“协议字段注册表、多格式能力矩阵和未来 PPTX 适配架构设计”是否合理。请先读根目录 `AGENTS.md`，再读上述 spec，并结合当前代码和长期规范判断它是否适合这个项目。

项目背景：
- 当前主线是固定语义 HTML 与 InDesign 的双向翻译。
- 未来可能加入 PPTX，让 HTML / InDesign / PPTX 之间都能通过统一语义模型互转。
- 我希望避免未来字段不断膨胀时各模块各写各的，因此提出 `src/protocol/` 字段注册表、能力矩阵、字段生命周期、格式适配器边界。
- 当前静态字段表还没删除，因为注册表还没实现；规范里已经标注“注册表落地前，静态字段表仍是当前行为事实源”。

请重点审查：
1. 这个 spec 的目标架构是否足够清晰，是否真的能避免 HTML/InDesign/PPTX 多格式之间两两互转失控。
2. canonical field / source metadata / format extension / observation field / retired field 这几类字段划分是否合理。
3. capability matrix 的 `native`、`lossless`、`approximate`、`fallback`、`observe-only`、`unsupported` 是否足够表达转换质量和风险。
4. 字段生命周期 active / observation / retired 的规则是否过于复杂或不够严格。
5. 对现有代码迁移顺序是否合理，是否有会导致大爆炸重构的风险。
6. 如果未来加入 PPTX，这个结构是否能扩展，还是需要提前改变边界。
7. 现有 `AGENTS.md` 和 `docs/规范/*` 里对这份 spec 的引用是否准确，是否有把“已设计”误写成“已实现”的风险。

输出要求：
1. 先给结论：建议采纳、局部调整后采纳、还是推倒重写。
2. 列出你实际查看到的关键文件、模块和数据流。
3. 按严重程度列出有证据的问题，必须给出文件路径或文档位置。
4. 如果你认为 spec 需要调整，请直接给出一版“修订后的 spec 大纲或完整替代 spec”。不要只给零散建议。
5. 如果你认为现有 spec 合理，也要指出哪些地方不应过度设计，哪些静态字段表暂时不应删除。
6. 给出渐进实施计划和验证建议。

不要修改代码。不要泛泛而谈。没有证据的判断请标明不确定。
```
