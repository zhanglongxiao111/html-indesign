# 协议字段注册表实施计划全量复审任务（第 4 轮）

## 背景

请续接同一审核线程，但本轮不要只看上一轮增量，也不要只检查已经指出过的问题。

用户明确指出：前一轮复审范围收窄会导致更容易获得通过。本轮必须重新做全量审核。

## 必读文件

请完整阅读并交叉核对：

- `AGENTS.md`
- `docs/superpowers/specs/2026-05-31-protocol-field-registry-architecture-design.md`
- `docs/superpowers/plans/2026-05-31-protocol-field-registry-implementation-plan.md`
- 当前仓库中 `src/`、`scripts/`、`test/` 与计划相关的真实文件结构和导出关系。

上一轮输出只作为历史参考，不能限制本轮审核范围：

- `docs/AI协作/本地Agent/进行中/2026-05-31_协议字段注册表计划OpenCode审核/输出_协议字段注册表计划审核_OpenCode_第1轮.md`
- `docs/AI协作/本地Agent/进行中/2026-05-31_协议字段注册表计划OpenCode审核/输出_协议字段注册表计划审核_OpenCode_第2轮.md`
- `docs/AI协作/本地Agent/进行中/2026-05-31_协议字段注册表计划OpenCode审核/输出_协议字段注册表计划审核_OpenCode_第3轮.md`

## 全量审核目标

请重新判断这份计划是否适合执行，重点包括但不限于：

- 是否覆盖规格中的字段注册表、能力矩阵、生命周期、字段分类、观察字段、退役字段、PPTX 预留、文档生成。
- 是否符合 `AGENTS.md` 的硬规则，尤其是退役代码必须清理、兜底默认有害、失败早于假成功、语义库白名单、事务所素材原位引用。
- 阶段顺序是否合理，有没有先删除后迁移、先强门禁后登记事实、先生成文档后覆盖字段事实等顺序错误。
- 第 4.2 节和第 8 阶段是否覆盖当前旧目录全部源文件，并且移动目标、公共导出、脚本导入、测试导入一致。
- 所有执行命令是否适合 Windows / PowerShell；不能使用 Unix-only 命令作为最终门禁。
- 是否存在硬编码旧路径、空扫通过、过度宽松 warning、静默吞错、保留旧 facade、保留 legacy 双路径等会制造假通过的问题。
- 是否存在明显缺少测试、验证门禁不够严、真实 InDesign E2E 触发时机不合理的问题。
- 是否有计划文本自身矛盾，例如前文说必须删除，后文又保留；前文说生成文档是事实源，后文又保留静态表；前文目标 API 与迁移清单不一致。

## 输出要求

请把全量复审结果写入：

- `docs/AI协作/本地Agent/进行中/2026-05-31_协议字段注册表计划OpenCode审核/输出_协议字段注册表计划审核_OpenCode_第4轮.md`

输出格式：

```markdown
# 审核结论

结论：通过 / 不通过

## 必改问题

- [如无，写“无”]

## 建议问题

- [如无，写“无”]

## 证据

- [引用具体文件路径和段落/关键词]
```

只有在整份计划没有必改问题时，才允许写“结论：通过”。建议问题不能阻止通过，但必须说明为什么不是必改。

## 边界

- 可以读取仓库文件。
- 不要修改任何代码或文档。
- 不要运行真实 InDesign。
- 不要提交 git。
- 用中文输出。
