# 协议字段注册表实施计划复审任务（第 2 轮）

## 背景

请续接上一轮审核。上一轮你输出“不通过”，主要指出：

1. `src/paged-html/` 和 `src/indesign-reverse/` 有多个文件没有迁移或退役归宿。
2. 第 4.2 节移动表与第 8 阶段 `git mv` 命令不一致。
3. 退役字段扫描测试硬编码旧路径，阶段 8 后可能空扫通过。

当前计划已根据这些意见修改：

- 计划文件：`docs/superpowers/plans/2026-05-31-protocol-field-registry-implementation-plan.md`
- 上一轮输出：`docs/AI协作/本地Agent/进行中/2026-05-31_协议字段注册表计划OpenCode审核/输出_协议字段注册表计划审核_OpenCode_第1轮.md`

## 复审目标

请重新审核计划文档，重点确认上一轮 3 个必改问题是否已经被真正解决，同时检查是否引入新的必改问题。

重点核对：

- 第 4.2 节是否覆盖当前 `src/paged-html/` 与 `src/indesign-reverse/` 中除旧 `index.js` facade 之外的所有文件。
- 第 4.2 节和第 8.3-8.5 的 `git mv` 命令是否一致。
- 第 6.5 节退役字段扫描是否同时覆盖旧路径和阶段 8 后的新路径，且不会因为旧目录不存在而空扫通过。
- 第 8.9 节是否明确更新当前 scripts/tests 对旧目录的依赖。
- 是否仍有 Windows / PowerShell 不可执行命令残留。

## 输出要求

请把复审结果写入：

- `docs/AI协作/本地Agent/进行中/2026-05-31_协议字段注册表计划OpenCode审核/输出_协议字段注册表计划审核_OpenCode_第2轮.md`

输出格式：

```markdown
# 审核结论

结论：通过 / 不通过

## 必改问题

- [如无，写“无”]

## 建议问题

- [如无，写“无”]

## 证据

- [引用具体文件路径和段落/关键词]
```

只有在没有必改问题时，才允许写“结论：通过”。建议问题不能阻止通过。

## 边界

- 可以读取仓库文件。
- 不要修改任何代码或文档。
- 不要运行真实 InDesign。
- 不要提交 git。
- 用中文输出。
