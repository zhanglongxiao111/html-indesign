# 协议字段注册表实施计划独立全量复审任务（新线程 1 第 2 轮）

## 背景

请续接新线程 1。上一轮你输出“不通过”，指出阶段 8 根 `index.js` 提前 require 阶段 10 才创建的 PPTX contracts。当前计划已修正：阶段 8 根入口不再导出 PPTX；阶段 10 创建 contracts 后再接入根公共入口。

此外，计划也吸收了部分建议：退役字段扫描更严格、根层历史模块增加预检、能力声明不能完全缺失。

## 必读文件

请完整阅读并交叉核对：

- `AGENTS.md`
- `docs/superpowers/specs/2026-05-31-protocol-field-registry-architecture-design.md`
- `docs/superpowers/plans/2026-05-31-protocol-field-registry-implementation-plan.md`
- 当前仓库中 `src/`、`scripts/`、`test/` 与计划相关的真实文件结构和导出关系。

不要只检查上轮差异；本轮仍然做全量审核。

## 审核目标

请重新判断这份计划是否适合执行。重点包括但不限于：

- 规格覆盖是否完整。
- 阶段顺序是否合理。
- 第 4.2 节和第 8 阶段移动清单是否覆盖全部旧目录文件。
- 阶段 8 和阶段 10 的公共入口是否不会出现阶段间 `MODULE_NOT_FOUND`。
- 执行命令是否适合 Windows / PowerShell。
- 是否存在硬编码旧路径、空扫通过、静默兜底、旧 facade 长期保留、legacy 双路径等假通过问题。

## 输出要求

请把审核结果写入：

- `docs/AI协作/本地Agent/进行中/2026-05-31_协议字段注册表计划OpenCode审核/输出_协议字段注册表计划审核_OpenCode_新线程1第2轮.md`

输出格式：

```markdown
# 审核结论

结论：通过 / 不通过

## 必改问题

- [如无，写“无”]

## 建议问题

- [如无，写“无”]

## 证据

- [引用具体文件路径和段落/关键词]
```

只有在整份计划没有必改问题时，才允许写“结论：通过”。建议问题不能阻止通过，但必须说明为什么不是必改。

## 边界

- 可以读取仓库文件。
- 不要修改任何代码或文档。
- 不要运行真实 InDesign。
- 不要提交 git。
- 用中文输出。
