# 协议字段注册表实施计划审核任务

## 背景

仓库：`D:\AI\html-indesign`

当前目标是审核这份计划文档：

- `docs/superpowers/plans/2026-05-31-protocol-field-registry-implementation-plan.md`

关联规格：

- `docs/superpowers/specs/2026-05-31-protocol-field-registry-architecture-design.md`
- `AGENTS.md`

本项目当前阶段允许较大范围架构重构；不要用“保守小步改”作为反对理由。关键是计划必须有清晰边界、阶段门禁、退役代码清理策略、Windows 可执行命令、实时 checklist 和可验证结果。

## 审核目标

请只审核计划文档本身是否适合执行，不要修改仓库文件，不要实现代码。

重点检查：

- 计划是否覆盖规格中的字段注册表、能力矩阵、生命周期、观察字段、退役字段、PPTX 预留和文档生成。
- 是否存在和 `AGENTS.md` 硬规则冲突的内容，尤其是退役代码、兜底字段、旧目录双入口、静默失败。
- 阶段拆分是否能执行，是否有明显顺序错误。
- 是否有不适合 Windows / PowerShell 环境的命令残留。
- 是否有文件路径、模块名、导出名明显不符合当前仓库的风险。
- 是否有计划文本写成“外部回复原文”而不是正式执行文档的残留。
- 是否有过度空泛、无法验证、或会诱导假通过的检查项。

## 输出要求

请把审核结果写入：

- `docs/AI协作/本地Agent/进行中/2026-05-31_协议字段注册表计划OpenCode审核/输出_协议字段注册表计划审核_OpenCode_第1轮.md`

输出格式必须包含：

```markdown
# 审核结论

结论：通过 / 不通过

## 必改问题

- [如无，写“无”]

## 建议问题

- [如无，写“无”]

## 证据

- [引用具体文件路径和段落/关键词]
```

只有在没有必改问题时，才允许写“结论：通过”。建议问题不能阻止通过。

## 边界

- 可以读取仓库文件。
- 不要修改任何代码或文档。
- 不要运行真实 InDesign。
- 不要提交 git。
- 用中文输出。
