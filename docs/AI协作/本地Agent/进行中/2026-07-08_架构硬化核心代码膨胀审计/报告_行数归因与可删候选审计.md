# 行数归因与可删候选审计

## 审计范围与取证

- 工作区：`D:\AI\html-indesign\.worktrees\architecture-hardening-guardrails`
- 基线范围：`5d4757a6f7efbe8c8410f816d462a90813567fb7..HEAD`
- 当前 `HEAD`：`d753a8407dbff2aa7c21805192ffcd0d429da46e`
- 主要命令：
  - `git diff --shortstat 5d4757a6f7efbe8c8410f816d462a90813567fb7..HEAD`
  - `git diff --numstat 5d4757a6f7efbe8c8410f816d462a90813567fb7..HEAD`
  - `git -c core.quotepath=false diff ... -- <file>`
  - `git log --oneline --no-merges 5d4757a6f7efbe8c8410f816d462a90813567fb7..HEAD`
  - `git ls-files '.superpowers/sdd/*.md'`
  - `git grep -n "\.superpowers" HEAD -- AGENTS.md docs package.json index.js src test scripts .gitignore`

## 总体结论

1. `[P1]` 本分支净增 `12068` 行（`14871` 增 / `2803` 删），但主要不是运行时代码无序膨胀，而是两类东西：`test/` 架构护栏与契约测试，和把脚本内逻辑收口到 `src/` 公共模块。
2. `[P1]` 最明确的可删候选不是 `src/`，而是被错误纳入版本库的 `.superpowers/sdd/*.md` 过程报告。它们净增 `1363` 行，`.gitignore` 已忽略 `.superpowers/`，仓库护栏也明确跳过该目录，且无运行时或文档入口引用。
3. `[P2]` `src/` 净增 `3001` 行总体合理：`scripts/` 反而净删 `547` 行，说明逻辑确实在向单一事实源收口，典型例子是 `conversion-gate`、`reverse-roundtrip`、`reverse-pipeline`、`indesign-pipeline`、registry-derived `protocol/constants`。
4. `[P2]` `test/` 净增 `8070` 行规模很大，但大部分能对应到明确边界：G1/G2/G3/G4/G5/G6/G7/G8 架构护栏、协议值域校验、adapter/writer strict contract。风险点不是“该不该有”，而是少数超大测试文件的维护集中度。

## 按目录归因

| 目录 | 文件数 | 新增 | 删除 | 净增 | 审计判断 |
| --- | ---: | ---: | ---: | ---: | --- |
| `test/` | 99 | 8478 | 408 | 8070 | 主要是 fail-closed、架构边界和协议契约测试；总体合理，但体量偏大。 |
| `src/` | 109 | 4206 | 1205 | 3001 | 主要是把原脚本内逻辑收口为可复用模块，并补 strict validation。 |
| `.superpowers/` | 14 | 1363 | 0 | 1363 | 明确过程产物，且当前已被 `.gitignore` 忽略；应从版本库删除。 |
| `other` | 9 | 559 | 412 | 147 | 含 `package.json`、`index.js`、规范生成物等，小幅净增。 |
| `docs/` | 2 | 95 | 61 | 34 | 主要是计划和规范同步，合理。 |
| `scripts/` | 11 | 170 | 717 | -547 | 包装层瘦身明显，说明共享逻辑在向 `src/` 迁移。 |

补充下钻：

- `test/architecture/` 单独净增 `4926` 行，是本轮最大增量来源。
- `src/protocol/` 单独净增 `1205` 行，是核心代码增量的最大来源。
- `.superpowers/sdd/` 单独净增 `1363` 行，是最明确的非产品性膨胀。

## 净增最大的前 20 个文件

| # | 文件 | + / - / 净增 | 判定 | 风险 | 证据与理由 |
| --- | --- | ---: | --- | --- | --- |
| 1 | `test/architecture/semantic-model-contract.test.js` | `1598 / 0 / 1598` | 必要新增 | P2 | G3 护栏文件，直接校验 adapter 出口必须 `validateSemanticModel`、HTML/ID reverse 字段同构、ratchet baseline。已接入 `npm test`。问题是过大，不是可删。 |
| 2 | `test/architecture/dependency-direction.test.js` | `517 / 0 / 517` | 必要新增 | P2 | G1 护栏，靠 `helpers/require-graph.js` 扫静态依赖，阻止 adapter/writer 反向依赖、cycle、`src` 依赖 `scripts`。 |
| 3 | `test/architecture/audit-fail-closed.test.js` | `501 / 0 / 501` | 必要新增 | P2 | G4 护栏，明确要求 audit CLI 对未知/缺失输入非零退出，并且要有 invalid-input 测试。属于 fail-closed 直接收益。 |
| 4 | `test/indesign-to-html/reverse-model.test.js` | `483 / 53 / 430` | 必要新增 | P2 | 旧 `indesign-reverse` 路径退役后，新文件承接 reverse model strict contract；新增点包括 label role 清洗、strict field validation、无 fallback 行为。 |
| 5 | `test/architecture/single-implementation.test.js` | `429 / 0 / 429` | 必要新增 | P2 | G6 护栏，约束共享 helper 只有一份实现，直接服务“单一事实源”。 |
| 6 | `test/architecture/helpers/require-graph.js` | `351 / 0 / 351` | 必要新增 | P2 | 供 G1/G8 共用的 require 图采集器，避免各测试各写一套扫描逻辑。属于收口，不是膨胀。 |
| 7 | `test/indesign-to-html/conversion-gate-cli.test.js` | `351 / 0 / 351` | 必要新增 | P2 | 明确断言 CLI 复用 `src/writers/html/audit/conversion-gate.js`，并验证预算、trusted source、invalid input 行为。 |
| 8 | `src/writers/html/audit/conversion-gate.js` | `321 / 0 / 321` | 必要新增 | P1 | 原 `scripts/audit-conversion-gate.js` 中大段核心逻辑已搬到这里，脚本只保留参数包装。典型“单一事实源”增量。 |
| 9 | `test/architecture/orphan-modules.test.js` | `299 / 0 / 299` | 必要新增 | P2 | G8 护栏，阻止 `src/` 出现无人拥有的孤儿模块；与“退役代码必须清理”一致。 |
| 10 | `test/architecture/protocol-literals.test.js` | `291 / 0 / 291` | 必要新增 | P2 | G2 护栏，阻止 `src/`/`scripts/` 继续散落 `data-id-*` 字面量，逼协议字段回到 `src/protocol/`。 |
| 11 | `test/semantic-model/to-instructions.test.js` | `273 / 6 / 267` | 必要新增 | P2 | Writer contract 扩充，覆盖 observed bounds、native table、effects、无 raw fallback dialect 等行为。 |
| 12 | `test/semantic-model/from-snapshot.test.js` | `269 / 4 / 265` | 必要新增 | P2 | Adapter contract 扩充，覆盖 strict field validation、retired path、observed guide/source metadata 等。 |
| 13 | `test/protocol/validate-model-fields.test.js` | `243 / 3 / 240` | 必要新增 | P1 | 直接验证 path/value-domain strict 校验，是 fail-closed 的核心保护层。 |
| 14 | `test/architecture/retired-naming.test.js` | `232 / 0 / 232` | 必要新增 | P1 | G5 护栏，阻止 `legacy`、`paged-html` 等退役命名回流。与项目“退役代码必须清理”硬规则完全一致。 |
| 15 | `.superpowers/sdd/task-10c-report.md` | `193 / 0 / 193` | 可删 | P1 | 过程报告，无运行时价值；`.gitignore` 已忽略 `.superpowers/`，`git grep` 只在忽略/跳过名单里命中该目录。该项代表同类 14 份报告。 |
| 16 | `src/writers/html/audit/reverse-roundtrip.js` | `192 / 0 / 192` | 必要新增 | P1 | 从 `scripts/indesign-e2e.js` 抽出 reverse HTML 语义检查、author package 审计、二次回环稳定性审计，属于脚本去重收口。 |
| 17 | `test/protocol/constants.test.js` | `189 / 0 / 189` | 必要新增 | P1 | 直接验证 `src/protocol/constants.js` 由 registry 派生，防止再次变成手写第二事实源。 |
| 18 | `src/protocol/scanners/scan-model-paths.js` | `191 / 7 / 184` | 需人工决定 | P2 | 当前增量有价值，因为 strict validation 需要稳定扫描模型表面；但它维护了较大的 path map，后续若继续膨胀，应评估进一步 registry 推导或按域拆分，避免隐性第二事实源。 |
| 19 | `test/architecture/docs-code-sync.test.js` | `171 / 0 / 171` | 必要新增 | P2 | G7 护栏，确保 `AGENTS.md` 的仓库地图和执行基线跟真实 `src/` 目录、audit script 同步。 |
| 20 | `src/protocol/constants.js` | `166 / 0 / 166` | 必要新增 | P1 | 由 `fieldRegistry` 派生协议常量和值域，直接服务“协议字段必须集中登记”和“单一事实源”。 |

### 对前 20 的补充判断

- 当前没有第二个像 `.superpowers/sdd/*.md` 这样明确的“大块可删”。
- 如需进一步压 LOC，应优先做“结构性收口”而不是删护栏：
  - `test/architecture/semantic-model-contract.test.js` 可在后续触及时按 G3 子规则拆分；
  - `src/protocol/scanners/scan-model-paths.js` 应避免继续平铺式扩张。

## 删除最多的前 20 个文件

| # | 文件 | + / - / 净增 | 是否证明旧实现已退役 | 说明 |
| --- | --- | ---: | --- | --- |
| 1 | `docs/规范/PROTOCOL_FIELD_REGISTRY.md` | `487 / 383 / 104` | 否 | 这是生成文档重算，不是退役。 |
| 2 | `scripts/indesign-e2e.js` | `16 / 276 / -260` | 是（部分） | 大量 helper 已抽到 `src/writers/indesign/audit/e2e-result-audit.js` 和 `src/writers/html/audit/reverse-roundtrip.js`；脚本入口仍保留。 |
| 3 | `scripts/audit-conversion-gate.js` | `13 / 203 / -190` | 是 | 核心 gate 构建/summary 逻辑已迁移到 `src/writers/html/audit/conversion-gate.js`，脚本只剩包装。 |
| 4 | `test/indesign-reverse/conversion-gate-cli.test.js` | `0 / 187 / -187` | 是 | 旧 `indesign-reverse` 路径退役，由 `test/indesign-to-html/conversion-gate-cli.test.js` 接替。 |
| 5 | `src/writers/indesign/instruction-writer.js` | `219 / 135 / 84` | 否 | 文件仍是主路径，删除只是重构成本。 |
| 6 | `scripts/indesign-reverse-export.js` | `1 / 112 / -111` | 是 | `compileReverseSnapshotToHtml` 已抽到 `src/reverse-pipeline/index.js`，脚本入口瘦身。 |
| 7 | `scripts/audit-reverse-visual.js` | `38 / 107 / -69` | 是（部分） | Playwright capture 和协议常量依赖已迁到 `src/adapters/html/reader/visual-geometry-capture.js` / `src/protocol`；脚本保留参数层。 |
| 8 | `src/adapters/html/reader/asset-detector.js` | `1 / 94 / -93` | 是（部分） | `placementFromAttributes` 迁到了 `src/shared/assets.js`，旧局部实现已退役。 |
| 9 | `src/writers/html/visual-html-writer.js` | `39 / 66 / -27` | 否 | 主文件仍在，只是精简。 |
| 10 | `src/adapters/html/reader/style-reader.js` | `0 / 62 / -62` | 是 | 旧 Node 侧 style picking 已删除，样式抓取改由 `browser-style-capture.js` 承担。 |
| 11 | `docs/superpowers/plans/2026-07-06-architecture-hardening-guardrails-implementation-plan.md` | `82 / 60 / 22` | 否 | 计划文档迭代，不是退役。 |
| 12 | `src/adapters/html/normalizer/snapshot-to-model.js` | `180 / 58 / 122` | 否 | 仍是主出口；删除来自 strict validation 与协议对齐重写。 |
| 13 | `test/indesign-to-html/reverse-model.test.js` | `483 / 53 / 430` | 否 | 当前文件仍在扩大，53 行删除只是重写过程。 |
| 14 | `src/writers/html/audit/content-inventory.js` | `91 / 40 / 51` | 否 | 仍是主路径，属收口后的持续迭代。 |
| 15 | `src/style-synthesis/index.js`（原 `src/writers/indesign/style-compiler.js`） | `32 / 37 / -5` | 是 | 旧 writer-local 路径已退役，职责迁到更中性的 `src/style-synthesis/`。 |
| 16 | `src/adapters/html/validators/authoring-validator.js` | `41 / 34 / 7` | 否 | 主文件仍在。 |
| 17 | `src/adapters/indesign/normalizer/snapshot-to-model.js` | `198 / 34 / 164` | 否 | 主文件仍在，删除来自 dialect 对齐和 strict 校验。 |
| 18 | `src/writers/html/author-node-attrs.js` | `32 / 31 / 1` | 否 | 主文件仍在。 |
| 19 | `src/writers/html/author-asset-attrs.js` | `30 / 29 / 1` | 否 | 主文件仍在。 |
| 20 | `src/style-synthesis/box-model.js`（原 `src/writers/indesign/box-model.js`） | `35 / 29 / 6` | 是 | 旧 writer-local box-model 路径已退役到共享样式合成层。 |

删除侧的总体判断：

- 好消息：大删减大多真的在做“退役代码清理”或“脚本去重收口”，不是表面移动后双路径并存。
- 未发现明显“旧实现仍保留在运行路径里”的强证据。

## 可删候选总表

| 候选 | 当前规模 | 风险 | 结论 | 证据 |
| --- | ---: | --- | --- | --- |
| `.superpowers/sdd/*.md` 已跟踪过程报告 14 份 | `1363` diff 新增行 | P1 | 应删 | `.gitignore` 已新增 `.superpowers/`；`git ls-files '.superpowers/sdd/*.md'` 仍能列出 14 个被跟踪文件；`git grep -n "\.superpowers"` 只命中 `.gitignore` 和 `test/architecture/*` 的 skip 列表，没有运行时或规范入口引用。 |

建议直接清理的具体文件：

- `.superpowers/sdd/final-review-fix-report.md`
- `.superpowers/sdd/task-10b-report.md`
- `.superpowers/sdd/task-10c-report.md`
- `.superpowers/sdd/task-10d-report.md`
- `.superpowers/sdd/task-10e-report.md`
- `.superpowers/sdd/task-11a-report.md`
- `.superpowers/sdd/task-11b-report.md`
- `.superpowers/sdd/task-11c-report.md`
- `.superpowers/sdd/task-11d-report.md`
- `.superpowers/sdd/task-11e-report.md`
- `.superpowers/sdd/task-12a-report.md`
- `.superpowers/sdd/task-12b-report.md`
- `.superpowers/sdd/task-12c-report.md`
- `.superpowers/sdd/task-12d-report.md`

## 不可删的主要理由

1. `test/architecture/**/*` 不是游离样例，而是已经被 `package.json` 的 `npm test` 显式纳入执行矩阵。
2. `scripts/*` 的大删减同时伴随 `src/` 中新增公共模块，说明这是去重收口，不是单纯搬家。
3. `src/protocol/constants.js` 这类新增不是第二事实源；相反，它通过 `fieldRegistry` 派生常量，并有 `test/protocol/constants.test.js` 保证来源唯一。
4. `test/semantic-model/*`、`test/protocol/*` 的新增直接对应 strict validation、retired path、allowedValues 等 fail-closed 行为；删掉会回到“能跑但不可信”的状态。

## 是否需要沉淀为长期规范

**需要，但只需补一条很窄的过程规范。**

建议沉淀：

- `.superpowers/` 仅作本地 scratch / review 包，不得作为版本库中的长期过程材料。
- 需要长期保留的 Agent 报告，进入 `docs/AI协作/本地Agent/`、`docs/review/` 或其他 `docs/` 既有过程目录。
- 一旦 `.superpowers/` 已被加入 `.gitignore`，同一轮必须把已跟踪的旧文件从版本库中移除；不能停在“以后它不会再新增”的半状态。

这条规范本质上是在执行现有项目原则，而不是另起一套新规则。
