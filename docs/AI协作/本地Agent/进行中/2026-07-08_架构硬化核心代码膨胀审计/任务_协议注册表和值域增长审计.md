# 任务：协议注册表和值域增长审计

## 背景

当前分支 `codex/architecture-hardening-guardrails` 相对基线 `5d4757a6f7efbe8c8410f816d462a90813567fb7` 核心代码净增明显。需要确认 `src/protocol/` 增长是否属于必要硬化，还是存在未退役旧路径、重复注册、局部 allowlist 或第二能力矩阵。

## 目标

只读审计 `src/protocol/` 及其调用点，判断协议注册表、字段生命周期、能力矩阵、allowedValues 校验和 scanner/validator 增长是否合理。

## 范围

- `src/protocol/`
- `src/semantic-model/validator.js`
- `src/adapters/*` 和 `src/writers/*` 中调用 protocol validator / constants / registry 的地方
- 相关测试：`test/protocol/`、`test/semantic-model/validator.test.js`、`test/architecture/semantic-model-contract.test.js`

## 硬约束

- 只读审计，不得修改代码、测试、配置、git index 或 HEAD。
- 只允许写自己的报告：`docs/AI协作/本地Agent/进行中/2026-07-08_架构硬化核心代码膨胀审计/报告_协议注册表和值域增长审计.md`
- 不得调用子 agent。
- 不跑真实 InDesign E2E。
- 严格执行项目原则：协议字段集中登记；兜底默认有害；退役字段不得留在结构化模型、编译链路、执行器或规范示例；不能接受兼容双路径。

## 建议命令

- `git diff --stat 5d4757a6f7efbe8c8410f816d462a90813567fb7..HEAD -- src/protocol test/protocol`
- `git diff -U8 5d4757a6f7efbe8c8410f816d462a90813567fb7..HEAD -- src/protocol`
- `rg "allowedValues|allowlist|styleRefs|sourceType|retired|lifecycle|capabilities|FORMATS" src/protocol src/adapters src/writers test/protocol`
- 可运行 targeted tests，但要记录命令和结果。

## 报告要求

报告必须包含：

- 当前结论：必要增长 / 可疑增长 / 发现退役未清理。
- 证据：文件行号、命令结果摘要。
- 风险分级：P0/P1/P2/none。
- 可删候选：每项说明为什么可删、删除风险、建议验证命令。
- 不可删理由：对主要新增模块说明为什么应保留。
- 是否需要沉淀为长期规范。
