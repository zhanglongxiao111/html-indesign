## 1. 定义协议
- [x] 1.1 起草构建指令格式（页、槽位、自由页元素字段）。
- [x] 1.2 在 build-engine 规范中补充需求与场景。

## 2. 实现 Node Builder
- [x] 2.1 实现 `src/builder.js`：解析通过校验的 HTML → 构建指令 JSON。
- [x] 2.2 支持固定母版的槽位填充（文本、图片）。
- [x] 2.3 支持自由页模板：根据 HTML 结构生成新元素的描述（位置、尺寸、类型、样式）。
- [x] 2.4 生成示例指令文件供 JSX 执行。

## 3. 实现 JSX 执行器
- [x] 3.1 开发 `_indesign_scripts/build_from_instructions.jsx`：按指令创建页面、应用母版、填充槽位。
- [x] 3.2 在自由页中按指令动态创建文本/图形框并设置样式/内容。

## 4. 测试与示例
- [x] 4.1 提供端到端示例：content.html → 指令 → InDesign 执行（输出结果描述）。
- [ ] 4.2 如需，补充最小回归用例（固定槽位 + 自由页各一）。
